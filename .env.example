# App Store Connect API key for scripts/testflight.sh. Copy to .env (git-ignored) and
# fill in; keep the .p8 outside the repository. Create a key under Users and Access >
# Integrations > App Store Connect API; the issuer ID is shown above the key list.
ASC_KEY_PATH=
ASC_KEY_ID=
ASC_ISSUER_ID=
